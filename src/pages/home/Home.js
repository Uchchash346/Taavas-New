import React, { Fragment } from 'react';
import Layout from '../../layouts/Layout.js';

const Home = () => {
    return (
        <Fragment>
            <Layout />
        </Fragment>
    );
};

export default Home;